I will search for existing translations in the codebase to ensure consistent terminology for "Git Repos" and "Schema."
I will read `docs/endpoints/projects/schema_zh_TW.mdx` to see how "Schema" is translated in existing documentation.
I will list the contents of `docs/endpoints` to check for a `git-repos` directory and its related files.
I will list the contents of `docs/endpoints/git-repos` to identify the files related to Git repositories.
I will search for "Git Repos" in the codebase to confirm the standard translation used in other files.
I will read `packages/web/public/locales/zh-TW/translation.json` to check for any existing translations for "Git repository" in the Traditional Chinese (Taiwan) locale.
I will read `docs/endpoints/overview_zh_TW.mdx` to see how the various endpoints are listed and if there are any existing translations for "Git Repos."
I will search for `docs.json` in the root directory to find the Mintlify navigation configuration and see how the endpoints are structured.
I will read `docs/docs.json` to find the sidebar configuration for "Git Repos."
I will check if `docs/admin-guide/guides/project-releases_zh_TW.mdx` exists.
I will search for "Git Sync" in the codebase to see if it has been translated elsewhere.
I will read `docs/endpoints/git-repos/schema.mdx` to see if there is any other content in the file that needs translation.
I will check if `docs/endpoints/git-repos/configure_zh_TW.mdx` exists.
I will read `docs/endpoints/git-repos/configure_zh_TW.mdx` to check its title translation.
I will read `docs/endpoints/flows/schema_zh_TW.mdx` to see how "Flows Schema" is translated.
I will search for `schema_zh_TW.mdx` in the `docs/endpoints/flows` directory.
I will read `docs/endpoints/connections/schema_zh_TW.mdx` to see how "Connections Schema" is translated.
I will read `docs/endpoints/projects/schema_zh_TW.mdx` to see how "Projects Schema" is translated.
I will read `docs/endpoints/pieces/schema_zh_TW.mdx` to see how "Pieces Schema" is translated.
I will read `README_zh_TW.md` to see how "Git Repository" is translated.
---
title: 'Git 儲存庫結構'
openapi-schema: git-repo
icon: cube
---