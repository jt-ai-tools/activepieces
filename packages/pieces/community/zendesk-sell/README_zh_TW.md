I will search for the `README.md` file of the `zendesk-sell` piece to translate it into Traditional Chinese (Taiwan).
I will search for the `zendesk-sell` directory in the `packages/pieces/` folder to locate the target piece.
I will read the `README.md` file of the `zendesk-sell` piece located in `packages/pieces/community/zendesk-sell`.
# pieces-zendesk-sell

## 構建

執行 `turbo run build --filter=@activepieces/piece-zendesk-sell` 來構建該函式庫。