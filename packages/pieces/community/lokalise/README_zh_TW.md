I will read the content of `packages/pieces/lokalise/README.md` to begin the translation.
I'll list the contents of `packages/pieces` to locate the `lokalise` directory.
I'll search for the `lokalise` directory within `packages/pieces/community`.
I'll read the content of `packages/pieces/community/lokalise/README.md` to begin the translation.
# pieces-lokalise

## 構建

執行 `turbo run build --filter=@activepieces/piece-lokalise` 來構建此函式庫。